CSS classes for rapid application development, implement great layouts in no time.

Sliced out so far:
	forms,
	buttons,
	transitions,
	dropdown,
	button-group,
	input-group,
	custom-forms,
	nav,
	navbar,
	card,
	breadcrumb,
	pagination,
	badge,
	jumbotron,
	alert,
	progress-bar,
	media,
	list-group,
	close-button,
	toasts,
	modal,
	tooltip,
	popover,
	carousel,
	spinners

1. Bootstrap source SCSS file in bsscssfiles.
2. The "bootstrap.scss" is the file determining what is in and out.
3. Run node buildcss.js.
4. Find minified file in dist.